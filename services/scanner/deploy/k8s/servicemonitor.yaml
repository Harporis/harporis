apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: harporis-scanner
  labels:
    app: harporis-scanner
spec:
  selector:
    matchLabels:
      app: harporis-scanner
  endpoints:
    - port: metrics
      interval: 15s
